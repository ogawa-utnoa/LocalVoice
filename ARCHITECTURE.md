# LocalVoice の設計

## 1. 全体像

```text
[Option+Space] GlobalShortcutManager (Carbon HotKey + NSEvent の予備監視)
      │
      ▼
VoiceInputCoordinator ── 状態: 待機 → 録音中 → 文字起こし中 → 整形中 → 挿入中 → 結果表示 → 待機
      │
      ├─ 録音 ──── AudioCaptureService (AVAudioEngine → 16kHz モノラル)
      │               └─ AudioBuffer (最大11分) → ChunkManager (30秒ごと・2秒重ね)
      │
      ├─ 文字起こし ─ TranscriptionQueue (actor) → WhisperService → whisper-cli (別プロセス)
      │
      ├─ 整形 ──── TextProcessingPipeline
      │               OverlapMerger → VocabularyNormalizer → FillerProcessor → RewriteDetector
      │               → LLMService → llama-completion (別プロセス) → EditGuard → IntegrityValidator
      │
      └─ 挿入 ──── AccessibilityService (クリップボード + ⌘V)
```

外部コマンドは必ず `ProcessRunner` 経由で動かします。標準入力を閉じ、出力を並行して読み、制限時間を過ぎたら強制終了するので、部品が固まってもアプリは止まりません。

## 2. 録音と文字起こしを分ける

話している途中で止まらずに済むよう、録音と文字起こしを別々に動かします。

```text
マイク録音:   [0秒 ═══════════════ 最大10分まで止まらない ═══════════════▶]
チャンク:     [0〜30秒]   [28〜60秒]   [58〜90秒] ...(2秒ずつ重ねて境目の言葉を落とさない)
文字起こし:      └ 約3.5秒    └ 約3.5秒 ...(録音中に裏で終わる)
```

- マイクの受け取り処理では重い処理をしない(変換してバッファに足すだけ)
- `TranscriptionQueue` は actor。チャンクを受け取った時点で処理中の印を立てるので、停止時に「残りを待たずに先へ進む」ことが起きない
- 無音のチャンクは Whisper に渡さない(Whisper は無音で「ご視聴ありがとうございました」などを作りがちなため)。よく出る作り文句が1チャンク丸ごとなら捨てる
- 停止したら、残りの音声を最後のチャンクにして、すべての文字起こしが終わるのを待つ

## 3. メモリ

- アプリ本体は常駐して数十MB。Whisper と LLM は処理のときだけ別プロセスで起動し、終われば OS がメモリを回収する
- Whisper と LLM は同時に動かない(文字起こしが終わってから整形する)
- 実測の最大メモリ: whisper large-v3-turbo q5_0 約870MB、Qwen2.5 1.5B Q4 約1.3GB

空きメモリに応じて、使うモデルを自動で切り替えます([Models/README.md](Models/README.md))。
切り替えても、録音した音声や文字起こしの結果は失われません。

## 4. 整形の流れ

```text
全チャンクの文字起こし
  → OverlapMerger        重ねた2秒分の重複を取り除いてつなぐ
  → VocabularyNormalizer 辞書の聞き間違いを正しい表記へ(「クロード」→ Claude)。単語の区切りを見て置き換える
  → FillerProcessor      「えーっと」「あのー」などを消す(「あの人」「まあまあ」は残す)
  → RewriteDetector      「火曜日、いや水曜日」→「水曜日」などの言い直しを反映
  → LLMService           Qwen2.5 1.5B で句読点・残ったつなぎ言葉を整える(長文は約500字ごと)
  → EditGuard            話した言葉が変わっていないか、ブロックごとに検査。不合格なら整形前の文を使う
  → IntegrityValidator   数字・日付・URL・メール・辞書の語が消えたり変わったりしていないか検査
  → TextNormalizer       句読点と改行の最終調整
```

### LLM に言い換えさせない工夫

小さな LLM は、頼まなくても言い換えや内容の追加をします(実測例: 「すべてこの後Claudeでやる予定です」→「これからClaudeで処理することにしています」)。

1. 指示文を「校正者として、つなぎ言葉の削除・言い直しの反映・句読点の3つだけ」に絞り、お手本を3つ付ける
2. 同じ音声からは同じ文が出るよう、ランダム性を切る(temperature 0)
3. `EditGuard` で、出力と入力の最長共通部分を取り、次の場合は却下する
   - 元に無い文字が8%を超えて増えた(言い換え・追加)
   - つなぎ言葉以外の言葉が3文字以上消えた(言い直しの合図「いや」「じゃなくて」などの近くは除く)
   - 漢字・カタカナ・英数字の語が消え、ほかの場所にも残っていない

LLM が失敗・時間切れ・却下になっても、整形前の文章(辞書とルールで整えたもの)を必ず入力します。

## 5. 入力欄への挿入

```text
1. 完成した文をクリップボードへ(どんな失敗でも文章が残るように)
2. 自動挿入がオフ / アクセシビリティ権限なし / パスワード欄 → ここで止めて画面で知らせる
3. ショートカットの修飾キー(Option など)が離されるのを待つ
4. ⌘V を送る(第一手段)
5. ⌘V のキーイベントを作れないときだけ、アクセシビリティ API で直接書き込む
```

⌘V を第一手段にしている理由: アクセシビリティ API での直接書き込みは、Chrome や Electron 製のアプリ(Slack・VS Code など)で「成功」を返しながら何も入らないことがあるためです。
「完成文をクリップボードに残す」をオフにしている場合は、貼り付けの後に元のクリップボードの中身へ戻します。

## 6. 失敗への備え

| 失敗 | 動き |
| :--- | :--- |
| マイク権限なし・モデルなし | 録音を始めず、画面に理由を出す |
| Whisper の失敗・時間切れ(120秒) | そのチャンクは空として順番を保つ。すべて失敗して文章が空なら、画面にエラーを出す |
| LLM の失敗・時間切れ | 整形前の文章を入力する |
| アクセシビリティ権限なし | クリップボードに保存し、許可の案内を出す |
| 10分に達した | 自動で停止して処理する |

## 7. プライバシー

- 音声・文字を外部に送らない。通信するのはセットアップ時のモデル取得だけ
- 一時的な音声ファイルは処理後に消す
- ログ(`~/Library/Logs/LocalVoice/app.log`)は時間・文字数・エラーだけ。話した内容は書かない

## 8. 検証

- `swift run LocalVoiceTests`: 部品ごとのテストに加え、読み上げ音声を使った「音声 → 文字起こし → 整形 → 最終文」の一気通し検査
- `python3 scripts/eval_asr.py`: モデル・辞書の組み合わせごとに文字誤り率を測る([BENCHMARK.md](BENCHMARK.md))
