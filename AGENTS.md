# LocalVoice — AIエージェント向けの手順(Claude Code / Codex 共通)

このリポジトリは、macOS 用の完全ローカル音声入力アプリです。利用者から「セットアップして」「インストールして」と頼まれたら、以下の手順で最後まで進めてください。

## セットアップの進め方

1. `./setup.sh` を実行する(Mac の確認 → whisper.cpp / llama.cpp の導入 → モデル取得 → ビルド → テスト → 起動)。
   - モデルは約1.7GB。メモリに余裕がない Mac 向けの予備モデルも入れる場合は `./setup.sh --full`(合計約2.7GB)。
2. 失敗したら、表示されたメッセージの対処を行って再実行する。
   - `Xcode Command Line Tools are missing` → 利用者に `xcode-select --install` を実行してもらう(画面での操作が必要)。
   - `Homebrew is required` → 利用者に https://brew.sh の手順で導入してもらう(パスワード入力が必要)。
   - モデルのダウンロード失敗・チェックサム不一致 → もう一度 `./scripts/download_models.sh` を実行する。
3. 完了したら、利用者に次の2つを自分で許可してもらうよう伝える(macOS の仕様で、アプリやエージェントからは許可できない)。
   - マイク: 初回の録音時に出る確認で「許可」。
   - アクセシビリティ: システム設定 > プライバシーとセキュリティ > アクセシビリティ で LocalVoice をオン。
4. 動作確認の方法を伝える: テキストエディットなどの入力欄にカーソルを置き、Option+Space → 話す → もう一度 Option+Space。
5. 辞書づくりを提案する(任意・下記)。
6. 「Mac の起動時に自動で立ち上げますか」と聞き、希望されたら `./scripts/install_login_item.sh` を実行する(やめるときは `--uninstall`)。

## 辞書づくり(利用者の同意を得てから)

利用者が普段使う固有名詞を辞書に入れると、文字起こしの精度が大きく変わります(実測: 本人の語で文字誤り率 32.2% → 5.1%)。

1. **先に断る**: 「Claude Code と Codex にあなたが過去に入力した文章から、よく使う言葉だけを抜き出します。文章は画面に出しません。よろしいですか」と聞き、同意が取れたときだけ進む。
2. `python3 scripts/suggest_dictionary.py` を実行する(読むのはローカルのファイルだけ。出力は単語と回数のみ)。
3. 候補から、**人名・社名・製品名・サービス名など、声に出して言う固有名詞**だけを選ぶ。コードの識別子、一般的な英単語、ファイル名は選ばない。
4. 選んだ語ごとに、音声認識が間違えそうな書き方(カタカナ読みなど)を付ける。例: `{"word": "Substack", "reading": "サブスタック"}`。
   - 読みが一般的な言葉と重なる語(「ライン」「ガス」など)は、`reading` を空にして表記のヒントだけにする。誤変換を招くため。
5. **一覧を利用者に見せて確認を取り**、`python3 scripts/suggest_dictionary.py --apply <選んだJSON>` で `~/.localvoice/dictionary.json` に追加する(既存の語は消さない。`.bak` を残す)。
6. アプリを再起動する(起動時に読み込むため): `pkill -f "LocalVoice.app/Contents/MacOS/LocalVoice"; open LocalVoice.app`

語を増やしすぎない。文字起こしのヒントは先頭150文字までで、**長くすると精度が落ちる**(実測: 9語で0.7%、17語で1.3%、37語で3.7%)。入りきらない語も、表記の自動修正には使われる。

## 守ること

- 音声・文字を外部に送る処理を足さない。クラウドの AI API を組み込まない(このアプリの前提)。
- すでに入っている Homebrew のパッケージを、利用者に聞かずに更新・再インストールしない。
- macOS の権限(マイク・アクセシビリティ)を代わりに許可しようとしない。手順を伝えるだけにする。
- ログ(`~/Library/Logs/LocalVoice/app.log`)に話した内容を書く変更をしない。

## 開発するとき

- テスト: `swift run LocalVoiceTests`(日本語の読み上げ音声 Kyoko がある Mac では、実音声での一気通し検査も走る)
- アプリの再ビルド: `./scripts/build_app.sh`
- **再ビルドするとアクセシビリティの許可が外れる**(仮の署名が毎回変わるため。設定画面ではオンに見えても効かない)。
  `tccutil reset Accessibility <バンドルID>` を利用者に実行してもらい、アプリ再起動後に許可し直す。バンドルIDの既定は `local.voice.LocalVoice`。
  恒久対策は、キーチェーンアクセスで作ったコード署名用の証明書名を `build.local.env` の `LV_SIGN_IDENTITY` に書くこと
  (証明書アシスタントは画面上部のメニュー「キーチェーンアクセス」>「証明書アシスタント」>「証明書を作成…」。種類はコード署名。
  証明書に「信頼」を設定しなくても署名でき、署名の要件が証明書ベースになるため再ビルドでも許可が外れない)。
- 文字起こし精度の比較: `python3 scripts/eval_asr.py`
- 個人用の辞書は `~/.localvoice/dictionary.json`(リポジトリの外)。社名・人名など公開したくない言葉はここに置く。
- 候補の抽出: `python3 scripts/suggest_dictionary.py`(本人の入力だけを読み、単語と回数だけを出す)
